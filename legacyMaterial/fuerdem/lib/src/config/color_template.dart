import 'package:flutter/material.dart';

class ColorTemplate {
  /// used for buttons & non-active texts
  static const Color darkBlue = Color(0xFF3F434A);

  /// used for active texts i.e links
  static const Color brightBlue = Color(0xFF2CBDFB);
}
